# 山东省国控企业管理有限公司消防与用电安全智能检查服务平台 Demo

## 项目目标

在短时间内完成一个可用于商务汇报和产品路演的高质量前端 Demo。Demo 用于展示“智能安全帽 + 大模型知识中台 + 远程专家 + 隐患闭环 + 报告中心 + 风险画像”的完整产品闭环。

该 Demo 是演示系统，不是生产系统。

## 技术栈要求

- 使用 React + Vite + TypeScript。
- 使用 Tailwind CSS 或普通 CSS Modules 完成界面。
- 图标使用 lucide-react。
- 图表使用 recharts。
- 状态管理优先使用 React 内置状态或 Zustand；不要引入复杂状态框架。
- 数据全部使用本地 mock data，不接后端。
- 不做真实登录，不做真实权限系统，不做真实上传，不接真实地图 API，不接真实 AI 接口。
- 不使用需要 API Key 的外部服务。
- 确保 `npm install`、`npm run dev`、`npm run build` 可以正常执行。

## 视觉风格

参考 `public/mockups` 下的 19 张图片。

整体风格：

- 深蓝色左侧导航栏。
- 白色内容区。
- 浅灰背景。
- 卡片式布局。
- 青绿色/蓝色作为主按钮色。
- 红、橙、黄、绿表示风险等级。
- 页面需要有政企级、SaaS 后台、数据看板的专业感。
- 页面文字使用中文。
- 所有数据均为演示数据，应在合适位置显示“演示数据”或“Demo”标识，避免被误解为真实监管系统。

## 业务定位

本系统是山东省国控企业管理有限公司面向企业、园区、物业、商业综合体和政府购买服务场景的消防与用电安全智能检查服务平台。

不要把系统写成“山东省应急管理厅监管平台”。不要输出行政处罚、执法结论等措辞。

系统输出应使用：

- 疑似隐患
- 建议核查
- 建议整改
- 建议专家复核
- 检查报告
- 整改建议书

避免直接使用：

- 违法
- 行政处罚
- 执法结论
- 官方监管认定

## 必须实现的演示闭环

1. 运营工作台：展示今日任务、待整改隐患、超期预警、在线安全帽、风险趋势。
2. 任务派发：选择项目、检查类型、检查员、安全帽设备，确认派发任务。
3. 智能安全帽现场检查：展示第一视角画面、设备状态、AI 疑似隐患、检查项、时间轴。
4. 大模型识别分析：展示图片识别、隐患图谱、检查依据、风险评分、整改建议。
5. 隐患登记：将 AI 识别结果转为隐患记录，补充责任单位、整改期限、整改措施。
6. 远程专家端：专家查看现场视频和证据，确认隐患、调整风险等级、给出专家意见。
7. 隐患闭环：展示待整改、整改中、复查中、专家复核、已销号等状态流转。
8. 报告中心：生成消防与用电安全检查报告，支持预览和模拟导出 PDF。
9. 风险画像：展示园区/项目隐患类型分布、风险等级分布、超期趋势和企业排行。

## 页面路由建议

- `/dashboard` 运营工作台
- `/tasks` 检查任务与任务派发
- `/helmet-live` 智能安全帽现场终端
- `/ai-analysis` 大模型知识中台
- `/hazards` 隐患闭环看板
- `/hazards/register` 隐患登记
- `/expert` 远程专家端
- `/reports` 报告中心
- `/analytics` 风险画像
- `/customers` 客户与项目档案
- `/devices` 设备管理
- `/templates` 检查模板管理
- `/scenarios/property` 物业与商业综合体巡检
- `/scenarios/park` 园区与厂中厂检查
- `/scenarios/hot-work` 动火作业与临时用电检查

如果时间不足，优先完成前 9 个页面，其余页面可以用 mockup 图片作为展示页。

## 数据要求

使用 `src/data/mockData.ts` 统一维护模拟数据。

示例项目：

- 齐鲁科技园
- 国控大厦
- 鲁商广场
- 高新智造产业园
- 银座佳驿酒店
- 鲁商物流园

示例人员：

- 张三：现场检查员
- 李四：项目安全员
- 王工：电气专家
- 赵工：消防安全专家
- 孙工：注册安全工程师

示例隐患：

- 配电箱未关闭
- 线缆裸露
- 灭火器压力不足
- 消防通道占用
- 应急照明故障
- 临时电源箱防护缺失
- 电焊机接地缺失
- 周边可燃物未清理
- 插排串接
- 配电室堆放杂物

## 交互要求

- 所有按钮都要有实际业务作用：更新数据/状态、筛选查询、重置表单、跳转页面、打开有内容的工作流弹窗、生成/导出/下载文件等；Toast 只能作为动作完成后的反馈，不能作为唯一实现。
- 所有日期、时间、期限、日期范围选择必须使用原生日期/时间控件（如 `input[type="date"]`、`input[type="datetime-local"]`、`input[type="time"]`），不要用普通下拉框或纯文本输入模拟日期。
- 任务派发点击“确认派发”后，任务状态变为“待执行”，并提示“任务已派发至智能安全帽”。
- 智能安全帽页面点击“生成隐患”后，跳转到隐患登记页。
- 大模型分析页面点击“生成整改建议”后，展示整改建议卡片。
- 隐患登记点击“提交隐患”后，进入隐患闭环看板。
- 远程专家点击“提交审核”后，隐患状态进入“整改中”。
- 整改复查页点击“提交复查”后，状态变为“已销号”。
- 报告中心点击“生成报告”后，展示报告预览；点击“导出PDF”显示模拟导出成功提示。
- 增加一个“演示模式”按钮，可以按顺序自动或手动跳转演示流程。

## 完成标准

- `npm run build` 无报错。
- 首页打开后视觉完整，无空白页面。
- 左侧导航、顶部栏、主要页面、核心按钮可用。
- Demo 主流程可以在 5 到 8 分钟内顺畅演示。
- `README.md` 中写明运行方式和推荐演示脚本。

## UI 布局质量要求

每次修改以下类型页面时，必须先阅读并遵守：

`docs/skills/ui-layout-quality.md`

适用页面类型：
- 后台看板页面
- 统计分析页面
- 数据分析页面
- 图表页面
- 风险画像页面
- 运营工作台页面
- 含 KPI、图表、排行、表格的管理页面

核心要求：
- KPI 指标卡必须使用固定列数 CSS Grid，禁止随意 flex-wrap。
- 图表卡片必须等高。
- 筛选栏、指标区、图表区、表格区必须使用统一 gap 和统一栅格。
- 1440px 和 1920px 下必须检查布局稳定性。
- 不允许出现单个指标卡或图表卡孤零零掉到下一行。

## UI Typography & Visual Polish

每次修改任何前端页面、表格、详情面板、表单、看板、导航、卡片组件前，必须阅读并遵守：

`docs/skills/ui-typography-quality.md`

尤其需要检查：
1. 不得滥用 `font-bold`、`font-extrabold`、`font-black`。
2. 不得大面积使用 `text-black` 或过深文字。
3. 表格正文必须使用 `13px / 400` 字重。
4. 表格长文本必须省略或限制两行，不得挤压成竖排。
5. 详情面板必须区分 label 和 value。
6. 页面标题、卡片标题、表头、正文、辅助文字必须有明确层级。
7. 主按钮统一蓝色。
8. 风险标签和状态标签统一使用轻量样式。
9. 修改完成后必须检查 1440px 和 1920px 下的视觉效果。
10. `npm run build` 必须通过。

如果发现页面视觉过重、文字过黑、表格换行严重，应优先修复 typography 和 table layout，而不是继续堆功能。

## UI Audit Checklist

每次完成页面开发后，必须根据 `docs/skills/ui-audit-checklist.md` 做自查，并在回复中说明：

- 修改了哪些 typography 问题；
- 修复了哪些 table layout 问题；
- 是否仍存在视觉风险；
- `npm run build` 是否通过。

## Project Skills Entry Rules

每次处理本项目需求前，先根据任务类型阅读对应 Skill：

- 前端看板、统计页、图表页、设备页、隐患页、模板页：阅读 `docs/skills/ui-layout-quality.md`。
- 前端文字、表格、详情面板、按钮、标签：阅读 `docs/skills/ui-typography-quality.md`。
- 页面完成后自查：阅读 `docs/skills/ui-audit-checklist.md`。
- 设备管理、隐患闭环、远程专家、业务按钮、状态流转：阅读 `docs/skills/safety-business-workflow.md`。
- 大模型中台、知识库、隐患图谱、专家规则库：阅读 `docs/skills/model-center-pages.md`。
- PPT 图片安插、ZIP 图片替换、遮罩、图片来源校验：阅读 `docs/skills/ppt-image-insertion-quality.md`。

执行原则：

- 不要只修当前截图暴露的问题，要检查同类页面是否有同样问题。
- 不改变既有路由，不删除业务数据。
- 主按钮统一蓝色。
- 风险标签统一为高风险红色、中风险橙色、低风险绿色。
- 所有主要按钮必须有 Toast、弹窗、状态变化、数据变化或路由跳转。
- 前端修改完成后运行 `npm run build`。
- PPT 修改完成后必须做页数、媒体数量、hash、总览图排除、遮罩范围和预览检查。
